import shared from 'belt/menu/js/menu-items/edit/shared';
import html from 'belt/menu/js/menu-items/inputs/driver/index.html';

export default {
    components: {
        driver: {
            props: ['menuItem'],
            methods: {
                wipe() {

                }
            },
            template: html,
        },
    },
}